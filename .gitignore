# Large InTAS dataset (clone separately from https://github.com/silaslobo/InTAS)
InTAS/

# Large cached SUMO traces (regenerated on first run)
results/intas_trace_*.npz

# KITTI raw data (~40 GB) and built caches — download/rebuild via sim/kitti_dataset.py
data/
results/kitti_*.npz

# Python
__pycache__/
*.pyc
*.pyo

# OS / editor
.DS_Store
.claude/
results/nuscenes_mm.npz
