# 오늘(2026-07-27 월) 남은 작업 — 핸드오프

## 자동으로 돌아가는 것 (건드릴 필요 없음)
- **18:42 KST** crontab: `window45.py evening45` — 평일 저녁 90분 수집(~530 스냅샷)
  → 6스킴 × 3시드 × **T=400** no-replay × KITTI+nuScenes. 로그 `newnewdata/evening45.log`,
  결과 `newnewdata/metrics_v2x_real_{kitti,nuscenes}_evening45.npz`. ~01시 완료 예상.
- **23:36 KST** crontab: `window45.py night45` — 심야판 동일. 화요일 아침 완료.
- 스크립트는 self-guarding(출력 npz 있으면 skip). GPU 충돌 없음(순차).

## 결과가 나오면 할 일 (화요일)
1. **트레이스 확인**: 로그의 `trace: N=..., K=...` — N(재적 코호트)·K(~530 기대).
   관측 단말 수(`coverage>=0.9 (of X)`)도 기록 → Sec V-A "차량 수" 문장 확정치.
   K<400이면 T=min(400,K)로 돌아갔는지 확인.
2. **고정 τ 확정**: 계획 = **KITTI 55%, nuScenes ~69%**(사전 고정 프레이밍).
   검증: 양 윈도우에서 전 스킴의 시드평균 곡선이 400라운드 내 τ 도달하는지
   (마지막 리뷰 지적 "사전 고정 동일 threshold" 대응). nuScenes 값은 플라토 보고
   조정 가능 — 단, 라운드 넘버(69 또는 70)로.
3. **테이블 재생성**: `newnewdata/make_compact_tables.py` 수정 필요:
   - npz 경로를 `*_evening45` / `*_night45`로
   - tau_mode="fixed" 추가(위 고정값), 도달 판정 = 시드평균 곡선
   - 하드코딩된 `>250` → `>400`
   - 기대: 전 셀 유한 숫자(400rd면 전원 도달), FACE Rounds 볼드,
     Comm은 Learning이 ~10% 쌀 수 있음 → 트레이드오프 문장으로 처리
     ("arrives XX rounds later to save 10% of the bytes").
4. **검증**: last-20 acc 순위(FACE 우위 복원 여부 — 90분·400rd·평일 코호트가
   토요일 압축 문제의 해법이었음), paired 시드 차이. 결과가 나쁘면 정직하게
   보고하고 기존 replay 테이블 유지 + 45분 윈도우는 로버스트니스로만.
5. **본문 수치 재산출**: "42.1% lower communication" 재계산, τ 문장
   ("fixed targets 55%/69%"), Sec V-A 차량 수·창 길이(원고가 "45-min, ~265
   snapshots, T=250"로 돼 있으면 **90-min, ~530, T=400으로 수정**),
   수집 시각(18:42–20:12 / 23:36–01:06).
6. **푸시**: 테이블 tex + npz + 원시 트레이스(`git add -f
   data/gangnam/seoul_v2x_trace_{evening45,night45}.npz`).
   ⚠️ 푸시엔 사용자 제공 GitHub PAT 필요(로컬 SSH 안 됨) — 새로 받을 것.
7. **크론 정리**: `crontab -e`에서 window45 관련 4줄(토요일분 25 7 * 2줄 +
   월요일분 27 7 * 2줄) 삭제 — 전부 1회성, 이미 발화됨.

## 컨텍스트 요약 (새 세션용)
- 테이블 현재 확정본: peak = τ_B(최강 베이스라인 최종치) FACE만 도달(187/62.7,
  228/77.7), off-peak = 95% τ. 비도달 행은 `>250 | >X GB` 하한 표기.
  고정 τ 400rd 버전이 완성되면 교체 여부를 사용자에게 보여주고 결정.
- **CFL-native는 테이블 행 금지**: AutoFed-CFL 71.2/86.9로 FACE를 이김(인프라
  1.3–1.5TB) → 본문 참조 문장으로만(각색 공정성 + 인프라 가격표).
  mmFedMC-CFL 45.1/64.8은 이식이 관대했다는 증거.
- 분해 사다리(리뷰 "caching 때문 아니냐" 대응): 캐싱단독(DTN 포함)≈NoComm,
  demand +1.4/+0.6, FACE 최상단 — KITTI 51.1/51.1–51.6/52.5/55.0/68.1,
  nuScenes 68.4/68.7–69.0/69.0/70.0/84.8.
- 유의성: 8시드 paired FACE−Learning +1.81pp, 8/8 전승, t p=0.0004.
- 준비된 스니펫: `overhead_snippet.tex`(메타데이터 0.012%, Γ 1.5ms/veh),
  `subsec_settings_details.tex`, `draft_snippets.tex`, README.md 참조.
- 원고 필수 수정 잔여: "encoded feature sizes"→"encoder parameter sizes",
  nuScenes 2클래스 명시, rich 배분은 규칙로 서술(≈80 고정 아님), τ 기호는
  γ 말고 τ(Γ와 충돌).
